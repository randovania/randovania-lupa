[tox]
envlist=
    py311

[testenv]
deps=
    Cython
    setuptools
passenv=
    SETUP_OPTIONS
commands=
    {envpython} setup.py --with-cython {env:SETUP_OPTIONS:} build install
    {envpython} -m randovania_lupa.tests.__main__
sitepackages=False
